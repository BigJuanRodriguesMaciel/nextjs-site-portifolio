import React from 'react'

type Props = {}

export default function MenuMobile({}: Props) {
  return (
    <div style={{height: '100vh', width: '100vw', backgroundColor: 'rgba(0,0,0,.7)', position: 'absolute', top: 0}}/>
  )
}